//将数组分成两部分，使得这两部分的和的差最小
//转换成使得这个子集中的元素的和尽可能接近sum/2，其中sum为数组中所有元素的和

function diff(arr){
    let len =arr.length;
    let sum=0;
    for (let i = 0; i < len; i++) {
        sum +=sum[i];
    }
    let arr1=[],arr2=[];
    for (let i = 0; i < len; i++) {
        for (let j = 0; j <sum/2; j++) {
           arr1
            
        }
        
    }
}
